# 원본 Fire/Smoke 데이터셋 구조 메모

다운로드한 데이터셋을 `--source` 로 지정하기 **전**에 아래를 채워 두면 팀 공유에 유리합니다.

## 원본 경로

- 로컬 절대 경로:

## 관측된 구조

- YAML 파일명 (`data.yaml` / `dataset.yaml`):
- `path` / `train` / `val` 키 유무:
- 이미지·라벨 디렉터리 (상대 경로):
- `names` 클래스 목록 (원본 id → 이름):

## 변환 필요 여부

- [ ] 폴더명만 `val` 로 통일하면 됨
- [ ] 라벨 클래스 remap 필요 (fire/smoke id 확인)
- [ ] YOLO txt 가 아닌 다른 포맷 → 별도 변환 스크립트 필요

## prepare 실행 로그 요약

(실행 후 콘솔 요약 또는 이슈 링크)
