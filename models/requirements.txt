# API server (run.py/app.main)
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic>=2.11.0
python-dotenv==1.0.1

# CV / inference
numpy>=1.26.0,<3
opencv-python>=4.8.0
ultralytics>=8.3.0
mss>=10.0.0

# 기존 stub 파이프라인 (/detect) — 필요 시
# mediapipe
